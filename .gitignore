# Virtualenv (legacy in-repo venv)
AiV_ProTool/
venv/
.venv/

# Build artefacts
build/
dist/
*.spec.bak

# Python caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime / logs
error.log
*.log

# Local INI configs (often contain machine-specific paths)
mim_converter_config.ini
*.local.ini

# OS / IDE
.DS_Store
Thumbs.db
.vscode/*.code-workspace

# Per-developer Claude Code overrides (team policy is in .claude/settings.json)
.claude/settings.local.json

# Native executables produced by partner tooling
mim2color.exe

# Installer build outputs (regenerated by installer/build.ps1)
installer/Output/
installer/version_info.txt

# PPT manual outputs (regenerated by tools/build_manual.py)
docs/AIVEX_Processing_Tool_v*.pptx
